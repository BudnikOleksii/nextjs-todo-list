import { type FC } from 'react';
import { type TodosQueryParams } from '@/types/todos-query-params';

interface Props {
  queryParams: TodosQueryParams;
}

export const Filers: FC<Props> = ({ queryParams }) => {
  return <div></div>;
};
